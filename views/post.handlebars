
<section>
  <h2>{{post.title}}</h2>
        <p>By <span class="">{{post.user.name}}</span> on {{format_date post.date_created}}</p>
        <div class="content">
        <p>{{post.content}}</p>
        </div>
        <div class="comment"><p>{{post.comment.content}}</p></div>
        <div id="submit-comment"></div><textarea></textarea><button>submit post</button></div>       
</section>